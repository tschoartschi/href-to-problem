import { hrefToX } from '../../../helpers/href-to-x';
import { module, test } from 'qunit';

module('Unit | Helper | href to x');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hrefToX(42);
  assert.ok(result);
});
